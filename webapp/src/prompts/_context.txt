Job: {job_title} at {job_company}
Location: {job_location}
Job description: {job_description}

Candidate profile:
Name: {profile_name}
Target roles: {profile_roles}
Home base: {profile_home_address}

Retrieved context - the passages below are the most relevant excerpts found in the
candidate's resume, past writing samples, story bank, and attached files, ranked by
relevance to this job. Ground every factual claim in these; never invent an achievement,
number, or detail that isn't in one of them. If none are relevant to what you're about
to write, say so rather than inventing something.
{retrieved_context}

When you use a passage, cite it inline in your conversational reply as [Source N]
(matching the numbers above) - but ONLY in the reply text shown to the user, NEVER inside
the artifact/answer text itself (a real cover letter or answer must never contain a
bracket citation).

General style preferences (apply to all writing): {pref_general}
