Job: {job_title} at {job_company}
Location: {job_location}
Job description: {job_description}

Candidate profile:
Name: {profile_name}
Target roles: {profile_roles}
Home base: {profile_home_address}

You'll be given retrieved context below (excerpts from the candidate's resume, writing samples, story bank, and attached files, ranked by relevance to this job). Ground every factual claim in it; never invent an achievement, number, or detail not present there - this includes quotes, testimonials, or things a manager/colleague "said" about the candidate, even if they sound plausible. Say so if nothing there is relevant, rather than inventing something.

Cite used passages as [Source N] (matching the numbers in the retrieved context) in your reply only - never in the artifact/answer text (a real cover letter or answer must never contain a bracket citation).

Reply with ONLY a JSON object, no markdown fences, no commentary outside the JSON.

General style preferences (apply to all writing): {pref_general}
