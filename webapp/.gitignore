# Secrets
.env

# Python
__pycache__/
*.pyc
.venv/
venv/
env/
*.egg-info/

# Flask
instance/
.flaskenv

# Uploaded resumes (user data, not sample data)
data/uploads/*
!data/uploads/.gitkeep

# SQLite database (user + progress state, not sample data)
data/app.db

# Rated chatbot responses (see src/store.py rate_chat_message) - user data, not sample data
data/results.json

# LLM call usage log (see src/agents.py _log_usage) - user data, not sample data
data/usage.json

# Chroma vector store (RAG chunk embeddings, rebuilt from data/app.db's documents table)
data/chroma/

# Debug: full prompt/response of the last LLM call (see src/agents.py _log_last_call)
data/last_llm_call.json

# OS / editor
.DS_Store
.vscode/
.idea/
